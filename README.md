# socket-to-http

Phase 1: 네트워크 기초 및 RAII 기반 소켓 코어 (Step 1 ~ 5)Step 1~3: C API 소켓, Echo Loop, RAII TcpSocket 클래스 구현 완료  Step 4: Non-blocking I/O 전환 (fcntl, O_NONBLOCK) 및 소켓 옵션 설정 (SO_REUSEADDR, TCP_NODELAY)Step 5: select / poll 기반 I/O 멀티플렉싱 구현 및 한계점 파악Phase 2: Linux 고성능 이벤트 루프 & 동시성 엔진 (Step 6 ~ 15)Step 6: epoll 코어 객체 추상화 (epoll_create1, epoll_ctl, epoll_wait)Step 7: Edge-Triggered(ET) vs Level-Triggered(LT) 동작 방식 비교 및 ET 모드 구현Step 8: EPOLLONESHOT 플래그를 활용한 멀티스레드 레이스 조건 방지Step 9: Reactor 패턴 구현 (EventLoop, Channel, Poller 구조 분리)Step 10: Min-Heap / Timer Wheel 기반 타이머 구현 (HTTP Keep-Alive Timeout 처리)Step 11: Thread-safe Task Queue 및 기본 Thread Pool 구현Step 12: Worker Thread별 독립 EventLoop 할당 (Multi-Reactor / Master-Worker 패턴)Step 13: Lock-free Ring Buffer 기반 작업 전달 엔진Step 14: CPU Affinity (pthread_setaffinity_np) 설정을 통한 캐시 적중률 극대화Step 15: eventfd / signalfd를 활용한 스레드 간 아키텍처 이벤트 통신Phase 3: 메모리 및 I/O 버퍼 관리 시스템 (Step 16 ~ 20)Step 16: 동적 가변 버퍼 (NetBuffer) 설계 (자동 확장, Read/Write Pointer 관리)Step 17: Zero-Copy 정적 파일 전송 (sendfile, splice 시스템 콜 적용)Step 18: Fixed-size Slab / Arena Allocator 메모리 풀 구현 (파편화 방지)Step 19: 연결 객체 재사용을 위한 Object Pool 패턴 구현Step 20: Scatter-Gather I/O (readv, writev) 적용Phase 4: HTTP/1.1 파서 및 프로토콜 엔진 (Step 21 ~ 30)Step 21: 결정적 유한 상태 머신(DFSM) 기반 HTTP Request Parser 설계Step 22: Request Line 파싱 (Method, URI, Version 추출 및 검증)Step 23: Header 파싱 및 Zero-copy String View (std::string_view) 적용Step 24: Body 수신 (Content-Length 및 Chunked Transfer Encoding 처리)Step 25: Multipart/form-data 파싱 (파일 업로드 지원)Step 26: HTTP Response Builder 구현 (상태 코드, MIME 타입 자동 설정)Step 27: Static File Handler 구현 (경로 탐색 공격 ../ 방어 보안 로직)Step 28: HTTP Keep-Alive 커넥션 재사용 파이프라인 완성Step 29: HTTP Pipelining 처리 및 순서 보장 큐 구현Step 30: HTTP 오류 처리 핸들러 및 사용자 정의 커스텀 에러 페이지 처리Phase 5: 웹 서버 프레임워크 & 프로토콜 확장 (Step 31 ~ 40)Step 31: Trie 자료구조 기반 라우팅 시스템 구현 (GET /user/:id)Step 32: 미들웨어 체인 패턴 구현 (Logging, CORS, Auth 통과 구조)Step 33: 초경량 C++ JSON Parser 연동 및 REST API 디스패처 작성Step 34: LRU Cache 기반 정적 파일 인메모리 캐싱 레이어Step 35: Dynamic Compression (Gzip / Brotli) 응답 필터 적용Step 36: OpenSSL 연동을 위한 SSL/TLS 핸드셰이크 레이어 추가 (HTTPS 지원)Step 37: WebSocket Handshake 프로토콜 구현 (Sec-WebSocket-Key 계산)Step 38: WebSocket Frame Parsing 및 양방향 통신 데몬 구현Step 39: SSE (Server-Sent Events) 단방향 실시간 스트리밍 지원Step 40: CGI / FastCGI 인터페이스 연동 (외부 스크립트 실행 환경)Phase 6: 엔터프라이즈 보안, 모니터링 & 최적화 (Step 41 ~ 50)Step 41: Token Bucket / Leaky Bucket 알고리즘 기반 Rate LimiterStep 42: DDoS / Slowloris 공격 방어 (연결 제한 및 읽기 타임아웃 강화)Step 43: 비동기 비블로킹 로거 (Lock-free RingBuffer + Dedicated Write Thread)Step 44: Graceful Shutdown 및 Hot Reloading (SIGHUP 신호 처리)Step 45: Daemonization 프로세스 전환 및 PID 파일 관리Step 46: Prometheus Exporter 규격의 서버 메트릭 측정 엔드포인트 (/metrics)Step 47: Linux Kernel Tuning (sysctl TCP 버퍼, SOMAXCONN, FILE-MAX)Step 48: CI/CD 파이프라인 구축 및 Docker 컨테이너화Step 49: wrk / ApacheBench 기반 부하 테스트 및 벤치마크 리포트 작성Step 50: AddressSanitizer(ASan), ThreadSanitizer(TSan), Valgrind를 통한 메모리 누수 및 데이터 레이스 제로 검증